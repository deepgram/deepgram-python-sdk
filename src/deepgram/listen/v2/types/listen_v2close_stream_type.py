# This file is NOT auto-generated by Fern — it is a hand-written compatibility shim.
#
# Background: the original Fern-generated ListenV2CloseStreamType erroneously allowed
# Union[Literal["Finalize", "CloseStream", "KeepAlive"], Any] — the v2 spec copied v1's
# control-message enum, but a v2 CloseStream message's `type` can only ever be
# "CloseStream" ("Finalize"/"KeepAlive" are separate messages). The 2026-06-15 spec fix
# narrowed the enum and Fern removed this type entirely.
#
# To preserve the public import path `ListenV2CloseStreamType` without re-introducing the
# bogus values, we recreate it here as the corrected single-value literal and freeze it in
# .fernignore so the generator won't delete it again.

import typing

ListenV2CloseStreamType = typing.Literal["CloseStream"]
